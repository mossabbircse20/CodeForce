#include<bits/stdc++.h>
using namespace std;
int main()
{
    int k,r; cin>> k >> r;
    int temp=0,i=1;
    while(true){
        temp+=k;
        if(temp%10 == 0 || temp%10 == r)
        {
            cout << i << endl;
            break;
        }
        else i++;continue;
    }
}
