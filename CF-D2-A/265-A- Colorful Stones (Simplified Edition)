#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s,ss; cin>>s; cin>>ss;
    int nxt=1,j=0;
    for(int i=0 ; i<ss.size() ; i++)
    {

        if(s[j]==ss[i])
        {
            nxt++;
            j++;
        }
    }
    cout << nxt << endl;
}
