#include <bits/stdc++.h>

using namespace std;

int main()

{
    int n,c=0; cin >> n;
    while(n--)
    {
        int p,t,v; cin >> p >> t >> v;
        if(p == 1 && t == 1 || p == 1 && v == 1 || t == 1 && v == 1)
        {
            c++;
        }
    }
    cout << c << endl;

}
