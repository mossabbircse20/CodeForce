#include <bits/stdc++.h>

using namespace std;

int main()
{
    char s1[100],s2[100];
    cin >> s1 >> s2;
    int eq = strcasecmp(s1,s2);
    if(eq == 0) cout << 0 << endl;
    else if(eq < 0) cout << -1 << endl;
    else cout << 1 << endl;
}
